<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="mostert.gerhard.googledrivesync.AddFolderActivity">

    <TextView
        android:id="@+id/localFolder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="30px"
        android:freezesText="true"
    />

    <Button
        android:id="@+id/selectLocalFolderButton"
        android:text="Choose Folder"
        android:onClick="chooseLocalFolder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/localFolder"
    />

    <TextView
        android:id="@+id/googleDriveFolder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="30px"
        android:layout_below="@id/selectLocalFolderButton"
        android:freezesText="true"
    />

    <Button
        android:id="@+id/selectGoogleDriveFolderButton"
        android:text="Choose Drive Folder"
        android:onClick="chooseGoogleDriveFolder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/googleDriveFolder"
    />

</RelativeLayout>
